interface:
  display_name: "Figma Parity"
  short_description: "Check whether a built site matches Figma"
  default_prompt: "Use $figma-parity to compare the requested pages and states with their exact Figma nodes, then report verified differences and missing comparisons."
