interface:
  display_name: "Website QA"
  short_description: "Check a website and verify the problems found"
  default_prompt: "Use $website-qa to check the requested pages, inspect the screenshots, verify important findings, and report what was and was not covered."
